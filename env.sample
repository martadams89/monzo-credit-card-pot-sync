# Flask configuration
FLASK_ENV=production
SECRET_KEY=change_me_in_production
DATABASE_URL=sqlite:///instance/app.db

# Server configuration
PORT=8000

# Gunicorn settings
GUNICORN_WORKERS=2
GUNICORN_THREADS=4
GUNICORN_TIMEOUT=120
GUNICORN_KEEP_ALIVE=5
GUNICORN_MAX_REQUESTS=1000
GUNICORN_MAX_REQUESTS_JITTER=100
LOG_LEVEL=info

# Application settings
MONZO_CLIENT_ID=
MONZO_CLIENT_SECRET=
TRUELAYER_CLIENT_ID=
TRUELAYER_CLIENT_SECRET=

# Email settings - Gmail example
# EMAIL_SMTP_SERVER=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_SMTP_USERNAME=your-email@gmail.com
# EMAIL_SMTP_PASSWORD=your-app-password  # Use an app password, not your Google account password
# EMAIL_FROM_ADDRESS=your-email@gmail.com
# EMAIL_USE_TLS=true

# Email settings - Local dev with MailHog
# EMAIL_SMTP_SERVER=localhost
# EMAIL_SMTP_PORT=1025
# EMAIL_SMTP_USERNAME=
# EMAIL_SMTP_PASSWORD=
# EMAIL_FROM_ADDRESS=dev@example.com
# EMAIL_USE_TLS=false

# Admin user configuration - for initial setup
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme_in_production
ADMIN_REGISTRATION_CODE=secure_registration_code

# Default application settings (can be changed in the UI)
DEFAULT_SYNC_INTERVAL_SECONDS=300
DEFAULT_DEPOSIT_COOLDOWN_HOURS=3
